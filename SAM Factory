-- IRON SMELTERS
-- SMELTERS ROW 1
local si1_1 = component.proxy("")
local si1_2 = component.proxy("")
local si1_3 = component.proxy("")
local si1_4 = component.proxy("")
local si1_5 = component.proxy("")
local si1_6 = component.proxy("")
local si1_7 = component.proxy("")
local si1_8 = component.proxy("")
local si1_9 = component.proxy("")
local si1_10 = component.proxy("")
local si1_11 = component.proxy("")
local si1_12 = component.proxy("")
local si1_13 = component.proxy("")
local si1_14 = component.proxy("")
local si1_15 = component.proxy("")
local si1_16 = component.proxy("")

-- SMELTERS ROW 2
local si2_1 = component.proxy("")
local si2_2 = component.proxy("")
local si2_3 = component.proxy("")
local si2_4 = component.proxy("")
local si2_5 = component.proxy("")
local si2_6 = component.proxy("")
local si2_7 = component.proxy("")
local si2_8 = component.proxy("")
local si2_9 = component.proxy("")
local si2_10 = component.proxy("")
local si2_11 = component.proxy("")
local si2_12 = component.proxy("")
local si2_13 = component.proxy("")
local si2_14 = component.proxy("")
local si2_15 = component.proxy("")
local si2_16 = component.proxy("")

-- CONTROL PANEL
-- SMELTERS ROW 1
local si1_1knob = panel:getModule(0, 0, 0)
local si1_1switch = panel:getModule(0, 0, 0)
local si1_1gauge = panel:getModule(0, 0, 0)
local si1_1nametag = panel:getModule(0, 0, 0)
local si1_1clock = panel:getModule(0, 0, 0)

local si1_2knob = panel:getModule(0, 0, 0)
local si1_2switch = panel:getModule(0, 0, 0)
local si1_2gauge = panel:getModule(0, 0, 0)
local si1_2nametag = panel:getModule(0, 0, 0)
local si1_2clock = panel:getModule(0, 0, 0)

local si1_3knob = panel:getModule(0, 0, 0)
local si1_3switch = panel:getModule(0, 0, 0)
local si1_3gauge = panel:getModule(0, 0, 0)
local si1_3nametag = panel:getModule(0, 0, 0)
local si1_3clock = panel:getModule(0, 0, 0)

local si1_4knob = panel:getModule(0, 0, 0)
local si1_4switch = panel:getModule(0, 0, 0)
local si1_4gauge = panel:getModule(0, 0, 0)
local si1_4nametag = panel:getModule(0, 0, 0)
local si1_4clock = panel:getModule(0, 0, 0)

local si1_5knob = panel:getModule(0, 0, 0)
local si1_5switch = panel:getModule(0, 0, 0)
local si1_5gauge = panel:getModule(0, 0, 0)
local si1_5nametag = panel:getModule(0, 0, 0)
local si1_5clock = panel:getModule(0, 0, 0)

local si1_6knob = panel:getModule(0, 0, 0)
local si1_6switch = panel:getModule(0, 0, 0)
local si1_6gauge = panel:getModule(0, 0, 0)
local si1_6nametag = panel:getModule(0, 0, 0)
local si1_6clock = panel:getModule(0, 0, 0)

local si1_7knob = panel:getModule(0, 0, 0)
local si1_7switch = panel:getModule(0, 0, 0)
local si1_7gauge = panel:getModule(0, 0, 0)
local si1_7nametag = panel:getModule(0, 0, 0)
local si1_7clock = panel:getModule(0, 0, 0)

local si1_8knob = panel:getModule(0, 0, 0)
local si1_8switch = panel:getModule(0, 0, 0)
local si1_8gauge = panel:getModule(0, 0, 0)
local si1_8nametag = panel:getModule(0, 0, 0)
local si1_8clock = panel:getModule(0, 0, 0)

local si1_9knob = panel:getModule(0, 0, 0)
local si1_9switch = panel:getModule(0, 0, 0)
local si1_9gauge = panel:getModule(0, 0, 0)
local si1_9nametag = panel:getModule(0, 0, 0)
local si1_9clock = panel:getModule(0, 0, 0)

local si1_10knob = panel:getModule(0, 0, 0)
local si1_10switch = panel:getModule(0, 0, 0)
local si1_10gauge = panel:getModule(0, 0, 0)
local si1_10nametag = panel:getModule(0, 0, 0)
local si1_10clock = panel:getModule(0, 0, 0)

local si1_11knob = panel:getModule(0, 0, 0)
local si1_11switch = panel:getModule(0, 0, 0)
local si1_11gauge = panel:getModule(0, 0, 0)
local si1_11nametag = panel:getModule(0, 0, 0)
local si1_11clock = panel:getModule(0, 0, 0)

local si1_12knob = panel:getModule(0, 0, 0)
local si1_12switch = panel:getModule(0, 0, 0)
local si1_12gauge = panel:getModule(0, 0, 0)
local si1_12nametag = panel:getModule(0, 0, 0)
local si1_12clock = panel:getModule(0, 0, 0)

local si1_13knob = panel:getModule(0, 0, 0)
local si1_13switch = panel:getModule(0, 0, 0)
local si1_13gauge = panel:getModule(0, 0, 0)
local si1_13nametag = panel:getModule(0, 0, 0)
local si1_13clock = panel:getModule(0, 0, 0)

local si1_14knob = panel:getModule(0, 0, 0)
local si1_14switch = panel:getModule(0, 0, 0)
local si1_14gauge = panel:getModule(0, 0, 0)
local si1_14nametag = panel:getModule(0, 0, 0)
local si1_14clock = panel:getModule(0, 0, 0)

local si1_15knob = panel:getModule(0, 0, 0)
local si1_15switch = panel:getModule(0, 0, 0)
local si1_15gauge = panel:getModule(0, 0, 0)
local si1_15nametag = panel:getModule(0, 0, 0)
local si1_15clock = panel:getModule(0, 0, 0)

local si1_16knob = panel:getModule(0, 0, 0)
local si1_16switch = panel:getModule(0, 0, 0)
local si1_16gauge = panel:getModule(0, 0, 0)
local si1_16nametag = panel:getModule(0, 0, 0)
local si1_16clock = panel:getModule(0, 0, 0)

-- SMELTERS ROW 2
local si2_1knob = panel:getModule(0, 0, 0)
local si2_1switch = panel:getModule(0, 0, 0)
local si2_1gauge = panel:getModule(0, 0, 0)
local si2_1nametag = panel:getModule(0, 0, 0)
local si2_1clock = panel:getModule(0, 0, 0)

local si2_2knob = panel:getModule(0, 0, 0)
local si2_2switch = panel:getModule(0, 0, 0)
local si2_2gauge = panel:getModule(0, 0, 0)
local si2_2nametag = panel:getModule(0, 0, 0)
local si2_2clock = panel:getModule(0, 0, 0)

local si2_3knob = panel:getModule(0, 0, 0)
local si2_3switch = panel:getModule(0, 0, 0)
local si2_3gauge = panel:getModule(0, 0, 0)
local si2_3nametag = panel:getModule(0, 0, 0)
local si2_3clock = panel:getModule(0, 0, 0)

local si2_4knob = panel:getModule(0, 0, 0)
local si2_4switch = panel:getModule(0, 0, 0)
local si2_4gauge = panel:getModule(0, 0, 0)
local si2_4nametag = panel:getModule(0, 0, 0)
local si2_4clock = panel:getModule(0, 0, 0)

local si2_5knob = panel:getModule(0, 0, 0)
local si2_5switch = panel:getModule(0, 0, 0)
local si2_5gauge = panel:getModule(0, 0, 0)
local si2_5nametag = panel:getModule(0, 0, 0)
local si2_5clock = panel:getModule(0, 0, 0)

local si2_6knob = panel:getModule(0, 0, 0)
local si2_6switch = panel:getModule(0, 0, 0)
local si2_6gauge = panel:getModule(0, 0, 0)
local si2_6nametag = panel:getModule(0, 0, 0)
local si2_6clock = panel:getModule(0, 0, 0)

local si2_7knob = panel:getModule(0, 0, 0)
local si2_7switch = panel:getModule(0, 0, 0)
local si2_7gauge = panel:getModule(0, 0, 0)
local si2_7nametag = panel:getModule(0, 0, 0)
local si2_7clock = panel:getModule(0, 0, 0)

local si2_8knob = panel:getModule(0, 0, 0)
local si2_8switch = panel:getModule(0, 0, 0)
local si2_8gauge = panel:getModule(0, 0, 0)
local si2_8nametag = panel:getModule(0, 0, 0)
local si2_8clock = panel:getModule(0, 0, 0)

local si2_9knob = panel:getModule(0, 0, 0)
local si2_9switch = panel:getModule(0, 0, 0)
local si2_9gauge = panel:getModule(0, 0, 0)
local si2_9nametag = panel:getModule(0, 0, 0)
local si2_9clock = panel:getModule(0, 0, 0)

local si2_10knob = panel:getModule(0, 0, 0)
local si2_10switch = panel:getModule(0, 0, 0)
local si2_10gauge = panel:getModule(0, 0, 0)
local si2_10nametag = panel:getModule(0, 0, 0)
local si2_10clock = panel:getModule(0, 0, 0)

local si2_11knob = panel:getModule(0, 0, 0)
local si2_11switch = panel:getModule(0, 0, 0)
local si2_11gauge = panel:getModule(0, 0, 0)
local si2_11nametag = panel:getModule(0, 0, 0)
local si2_11clock = panel:getModule(0, 0, 0)

local si2_12knob = panel:getModule(0, 0, 0)
local si2_12switch = panel:getModule(0, 0, 0)
local si2_12gauge = panel:getModule(0, 0, 0)
local si2_12nametag = panel:getModule(0, 0, 0)
local si2_12clock = panel:getModule(0, 0, 0)

local si2_13knob = panel:getModule(0, 0, 0)
local si2_13switch = panel:getModule(0, 0, 0)
local si2_13gauge = panel:getModule(0, 0, 0)
local si2_13nametag = panel:getModule(0, 0, 0)
local si2_13clock = panel:getModule(0, 0, 0)

local si2_14knob = panel:getModule(0, 0, 0)
local si2_14switch = panel:getModule(0, 0, 0)
local si2_14gauge = panel:getModule(0, 0, 0)
local si2_14nametag = panel:getModule(0, 0, 0)
local si2_14clock = panel:getModule(0, 0, 0)

local si2_15knob = panel:getModule(0, 0, 0)
local si2_15switch = panel:getModule(0, 0, 0)
local si2_15gauge = panel:getModule(0, 0, 0)
local si2_15nametag = panel:getModule(0, 0, 0)
local si2_15clock = panel:getModule(0, 0, 0)

local si2_16knob = panel:getModule(0, 0, 0)
local si2_16switch = panel:getModule(0, 0, 0)
local si2_16gauge = panel:getModule(0, 0, 0)
local si2_16nametag = panel:getModule(0, 0, 0)
local si2_16clock = panel:getModule(0, 0, 0)



-- PANEL CONTROLS



-- Action
-- event.listen(button1,button2,testswitch)


-- Constructor Clock
-- si1_1:setPotential(OC/100)

-- OC Knob
-- potent.min = 1
-- potent.max = 250
-- potent:setColor (0,1,0,.5)


