-- MACHINES
-- COMPUTER
local computer = component.proxy("")

-- IRON SMELTERS
-- SMELTERS ROW 1
local si1.1 = component.proxy("")
local si1.2 = component.proxy("")
local si1.3 = component.proxy("")
local si1.4 = component.proxy("")
local si1.5 = component.proxy("")
local si1.6 = component.proxy("")
local si1.7 = component.proxy("")
local si1.8 = component.proxy("")
local si1.9 = component.proxy("")
local si1.10 = component.proxy("")
local si1.11 = component.proxy("")
local si1.12 = component.proxy("")
-- SMELTERS ROW 2
local si2.1 = component.proxy("")
local si2.2 = component.proxy("")
local si2.3 = component.proxy("")
local si2.4 = component.proxy("")
local si2.5 = component.proxy("")
local si2.6 = component.proxy("")
local si2.7 = component.proxy("")
local si2.8 = component.proxy("")
local si2.9 = component.proxy("")
local si2.10 = component.proxy("")
local si2.11 = component.proxy("")
local si2.12 = component.proxy("")



-- CONTROL PANEL
-- SMELTERS ROW 1
local si1.1knob = panel:getModule(xaddress,yaddress)
local si1.1switch = panel:getModule(xaddress,yaddress)
local si1.1gauge = panel:getModule(xaddress,yaddress)

local si1.2knob = panel:getModule(xaddress,yaddress)
local si1.2switch = panel:getModule(xaddress,yaddress)
local si1.2gauge = panel:getModule(xaddress,yaddress)

local si1.3knob = panel:getModule(xaddress,yaddress)
local si1.3switch = panel:getModule(xaddress,yaddress)
local si1.3gauge = panel:getModule(xaddress,yaddress)

local si1.4knob = panel:getModule(xaddress,yaddress)
local si1.4switch = panel:getModule(xaddress,yaddress)
local si1.4gauge = panel:getModule(xaddress,yaddress)

local si1.5knob = panel:getModule(xaddress,yaddress)
local si1.5switch = panel:getModule(xaddress,yaddress)
local si1.5gauge = panel:getModule(xaddress,yaddress)

local si1.6knob = panel:getModule(xaddress,yaddress)
local si1.6switch = panel:getModule(xaddress,yaddress)
local si1.6gauge = panel:getModule(xaddress,yaddress)

local si1.7knob = panel:getModule(xaddress,yaddress)
local si1.7switch = panel:getModule(xaddress,yaddress)
local si1.7gauge = panel:getModule(xaddress,yaddress)

local si1.8knob = panel:getModule(xaddress,yaddress)
local si1.8switch = panel:getModule(xaddress,yaddress)
local si1.8gauge = panel:getModule(xaddress,yaddress)

local si1.9knob = panel:getModule(xaddress,yaddress)
local si1.9switch = panel:getModule(xaddress,yaddress)
local si1.9gauge = panel:getModule(xaddress,yaddress)

local si1.10knob = panel:getModule(xaddress,yaddress)
local si1.10switch = panel:getModule(xaddress,yaddress)
local si1.10gauge = panel:getModule(xaddress,yaddress)

local si1.11knob = panel:getModule(xaddress,yaddress)
local si1.11switch = panel:getModule(xaddress,yaddress)
local si1.11gauge = panel:getModule(xaddress,yaddress)

local si1.12knob = panel:getModule(xaddress,yaddress)
local si1.12switch = panel:getModule(xaddress,yaddress)
local si1.12gauge = panel:getModule(xaddress,yaddress)

-- SMELTERS ROW 2
local si2.1knob = panel:getModule(xaddress,yaddress)
local si2.1switch = panel:getModule(xaddress,yaddress)
local si2.1gauge = panel:getModule(xaddress,yaddress)

local si2.2knob = panel:getModule(xaddress,yaddress)
local si2.2switch = panel:getModule(xaddress,yaddress)
local si2.2gauge = panel:getModule(xaddress,yaddress)

local si2.3knob = panel:getModule(xaddress,yaddress)
local si2.3switch = panel:getModule(xaddress,yaddress)
local si2.3gauge = panel:getModule(xaddress,yaddress)

local si2.4knob = panel:getModule(xaddress,yaddress)
local si2.4switch = panel:getModule(xaddress,yaddress)
local si2.4gauge = panel:getModule(xaddress,yaddress)

local si2.5knob = panel:getModule(xaddress,yaddress)
local si2.5switch = panel:getModule(xaddress,yaddress)
local si2.5gauge = panel:getModule(xaddress,yaddress)

local si2.6knob = panel:getModule(xaddress,yaddress)
local si2.6switch = panel:getModule(xaddress,yaddress)
local si2.6gauge = panel:getModule(xaddress,yaddress)

local si2.7knob = panel:getModule(xaddress,yaddress)
local si2.7switch = panel:getModule(xaddress,yaddress)
local si2.7gauge = panel:getModule(xaddress,yaddress)

local si2.8knob = panel:getModule(xaddress,yaddress)
local si2.8switch = panel:getModule(xaddress,yaddress)
local si2.8gauge = panel:getModule(xaddress,yaddress)

local si2.9knob = panel:getModule(xaddress,yaddress)
local si2.9switch = panel:getModule(xaddress,yaddress)
local si2.9gauge = panel:getModule(xaddress,yaddress)

local si2.10knob = panel:getModule(xaddress,yaddress)
local si2.10switch = panel:getModule(xaddress,yaddress)
local si2.10gauge = panel:getModule(xaddress,yaddress)

local si2.11knob = panel:getModule(xaddress,yaddress)
local si2.11switch = panel:getModule(xaddress,yaddress)
local si2.11gauge = panel:getModule(xaddress,yaddress)

local si2.12knob = panel:getModule(xaddress,yaddress)
local si2.12switch = panel:getModule(xaddress,yaddress)
local si2.12gauge = panel:getModule(xaddress,yaddress)


-- Action
event.listen(button1,button2,testswitch)


-- Constructor Clock
si1.1:setPotential(OC/100)

-- OC Knob
potent.min = 1
potent.max = 250
potent:setColor (0,1,0,.5)


